nsign is linux based an image creation and signature tool.

It is used to create bootchain image from individual components
(ddr_fw.bin, second_boot_fw.bin, fw_payload.bin).

Usage: nsign <config_file>

e.g. ./nsign default.cfg
